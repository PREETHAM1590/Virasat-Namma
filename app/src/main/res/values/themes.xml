<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Virasat" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Instant window background matching app color - eliminates white flash -->
        <item name="android:windowBackground">@color/splash_bg</item>
        <!-- Disable window animations for faster perceived start -->
        <item name="android:windowDisablePreview">false</item>
    </style>

    <color name="splash_bg">#FFF5E6</color>
</resources>
